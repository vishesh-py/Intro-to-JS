// First of all, all the js code should be written in a separate file with the extension .js. 
// This is because it is easier to manage and maintain the code when it is separated from the HTML.

// To include the js file in the HTML, we use the <script> tag with the src attribute pointing to the js file.
// The <script> tag should be placed at the end of the body section of the HTML file to ensure that the HTML elements are loaded before the js code is executed.

// Example:
// <script src="setup.js"></script>

/*
Step to run the js code:
1. Create a new file with the extension .js (e.g., setup.js).
2. Write your js code in the setup.js file.
3. Include the setup.js file in your HTML file using the <script> tag with the src attribute.
4. Open the HTML file in a web browser to see the output of your js code.

Method 2:

Install node.js on your computer. Node.js is a JavaScript runtime that allows you to run JavaScript code outside of a web browser.

Step to run the js code using node.js:
1. Create a new file with the extension .js (e.g., setup.js).
2. Write your js code in the setup.js file.
3. Open a terminal or command prompt and navigate to the directory where the setup.js file is located.
4. Run the command `node setup.js` to execute the js code in the terminal.
*/